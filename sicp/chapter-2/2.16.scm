;; I imagine it is a difficult problem, since it's valid to complicate an expression by adding more (opposite) operations.
;; If you could simplify the expression to a simplest possible one algebraically, that would minimize the error bounds and could
;; solve the issue of different representations producing different results.
;; I guess this would require some symbolic manipulation or otherwise more complex representation of the intervals to be processed.
;; You would have to know which intervals are equal.